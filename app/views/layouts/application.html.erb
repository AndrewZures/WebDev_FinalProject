<!DOCTYPE html>
<html>
<head>
  <title>JustLikePinterest</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%=# javascript_include_tag :defaults %>
  <%= csrf_meta_tags %>
</head>
<body>
	<header>
	</header>
	<nav>
		    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="#">Just Like Pinterest</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
       		<% if session[:id] %>
	              <li class="active"><%= link_to "Home", user_url(session[:id]) %></li>
	              <li><%= link_to "Sign Out", sign_out_url, method: :delete %></li>
	        <% else %>
					<li><%= link_to "Sign In", sign_in_url %></li>
			<% end %>    
              <% if session[:id] %>
              <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle='dropdown'><i class="icon-home" id="nav-home-icon"></i>
                <b class="caret"></b></a>
                <ul class = "dropdown-menu">
                    <li><%= link_to "Profile", user_url(session[:id])%></li>
                    <li><%= link_to "Boards", boards_url(:id => session[:id])%></li>
                    <li><%= link_to "Friends", friends_index_url(:id => session[:id])%></li>
                    <li><%= link_to "Favorites", favorites_url(:id => session[:id])%></li>
                  </ul>
              </li> 
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
	</nav>
</div>
<br><br>
<%= yield %>

</body>

<div id="footer">
      <div class="container">
        <p class="muted credit">Example courtesy <a href="http://martinbean.co.uk">Martin Bean</a> and <a href="http://ryanfait.com/sticky-footer/">Ryan Fait</a>.</p>
      </div>
    </div>

</html>
