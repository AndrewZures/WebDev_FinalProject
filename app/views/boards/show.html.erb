<p id="notice"><%= notice %></p>

<p>
  <b>User:</b>
  <%= @user.name %>
</p>

<!--
<p>
  <b>Pin:</b>
  <%#= @board.pin_id %>
</p>
-->

<p>
  <b>Name:</b>
  <%= @board.name %><br>
  <%= link_to "Add Pin", new_pin_url(:board_id => @board.id) %><br>
  <%= link_to 'Edit Here', edit_board_path(@board) %> |
  <%= link_to 'Back', boards_path %>
</p>

<p>
  <b>Category:</b>
  <%= @board.category %>
</p>

  <% count = 0 %>
	<% @board.pins.each do |bp| %>
    <% if count < 4 %>
      <% count += 1 %>
    <%else %>
      <% count = 0 %>
      <br>
    <% end %>
      <%= link_to image_tag(bp.url, :height => 200, :width => 200), board_pin_url(@board.board_pins.find_by_pin_id(bp.id).id) %> </li>
	<% end %>
</ul>



